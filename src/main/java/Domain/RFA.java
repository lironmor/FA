package Domain;

public class RFA extends User{

    public RFA(String fullName, String email, String userName, String password) throws Exception {
        super(fullName, email, userName, password);
    }
}
